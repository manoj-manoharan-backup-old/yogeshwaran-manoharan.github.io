[{"title":"When and How to Use XHP Categories","description":"<p>I remember when we first added the <code>children</code> keyword to XHP. We had the problem that validating them was really cumbersome. Nearly every element was valid inside a <code>&lt;div&gt;</code> but not all (for instance <code>&lt;meta&gt;</code>). Listing out every valid child wasn&rsquo;t very elegant and certainly would cause problems for custom XHP components. Fortunately, the HTML spec categorized elements just for this purpose. Grouping elements into &ldquo;block&rdquo; or &ldquo;inline&rdquo; categories made validation far simpler. But if you&rsquo;ve used XHP you might be wondering, &ldquo;Why have I never needed to define my elements as inline or block before?&rdquo; Well, the answer lies in how XHP renders an element tree. First it will render down all your custom elements into their eventual HTML primitives, then it will render the entire HTML primitive tree into a text string. The key here is that there are actually two passes, meaning XHP validates children in two","link":"https://codebeforethehorse.tumblr.com/post/65461659692","owner":"Stefan Parker"}]